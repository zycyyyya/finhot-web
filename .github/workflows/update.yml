name: Manual Update

on:
  workflow_dispatch:  # 手动触发

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Data updates handled by WorkBuddy automation (build_data.py). Manual trigger only."
